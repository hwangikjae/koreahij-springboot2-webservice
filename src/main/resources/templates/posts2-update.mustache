{{>layout/header}}

<h1>게시글 수정</h1>

<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div class="form-group">
                <label for="title">글 번호</label>
                <input type="text" class="form-control" id="id" value="{{post2.id}}" readonly><!--머스테치는 객체의 필드 접근시 점(dot)으로 구분한다. 즉 Post클래스의 id에 대한 접근은 post.id로 사용할 수 있다.-->
            </div>
            <div class="form-group">
                <label for="title">제목</label>
                <input type="text" class="form-control" id="title2" value="{{post2.title2}}">
            </div>
            <div class="form-group">
                <label for="author"> 작성자 </label>
                <input type="text" class="form-control" id="author2" value="{{post2.author2}}" readonly>
            </div>
            <div class="form-group">
                <label for="content"> 내용 </label>
                <textarea class="form-control" id="content2">{{post2.content2}}</textarea>
            </div>
        </form>
        <a href="/" role="button" class="btn btn-secondary">취소</a>
        <button type="button" class="btn btn-primary" id="btn-update2">수정 완료</button>
        <button type="button" class="btn btn-danger" id="btn-delete2">삭제</button>
    </div>
</div>

{{>layout/footer}}